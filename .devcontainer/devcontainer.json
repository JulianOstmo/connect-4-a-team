{
  "name": "Node.js & TypeScript",
  "build": {
    "dockerfile": "Dockerfile"
  },
  // "features": {},
  // "forwardPorts": [],
  "postCreateCommand": "bash -c 'git config --global --add safe.directory /workspaces/connect-4-a-team && npm install'",
  "customizations": {
    "vscode": {
      "extensions": [
        "andys8.jest-snippets",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "ms-vsliveshare.vsliveshare",
        "xabikos.JavaScriptSnippets"
      ]
    }
  }
  // "remoteUser": "root"
}
